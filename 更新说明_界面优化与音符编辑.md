# 界面优化与音符编辑功能更新说明

## 更新时间
2026-09-12

## 一、演奏控制界面优化

### 问题
- 窗口缩小时组件发生重叠
- 缺少滚动条,内容无法完整显示
- 布局不响应窗口大小变化

### 解决方案
1. **添加滚动区域**
   - 使用 `QScrollArea` 包裹所有内容
   - 设置 `widgetResizable=True` 自动适应内容
   - 禁用水平滚动条,只保留垂直滚动

2. **响应式布局**
   - 所有卡片使用统一的间距和边距
   - 窗口缩小时自动出现滚动条
   - 保持所有控件可见和可操作

### 文件修改
- `gui/player_tab.py` (第175-213行)

## 二、音符编辑功能

### 新增功能
支持在简谱识别后对音符进行精细编辑:

1. **插入半拍休止符**
   - ← 前插半拍休止:在选中音符前插入 `0_`
   - 后插半拍休止 →:在选中音符后插入 `0_`
   - 自动处理空格,保持简谱格式整洁

2. **删除音符**
   - 删除选中音符:移除指定的音符或和弦
   - 自动清理相邻空白符
   - 保持多行简谱结构

3. **智能编辑**
   - 编辑后自动重新解析到表格
   - 自动选中新插入或相邻的音符
   - 同步更新简谱文本和表格视图

### 核心逻辑

#### `core/jianpu_editor.py`
简谱编辑器类,提供位置稳定的编辑操作:

```python
class JianpuEditor:
    def event_ranges(text: str) -> List[Tuple[int, int]]
        # 提取所有音符事件的字符范围
    
    def insert_rest(text: str, event_index: int, before: bool, rest_token: str) -> str
        # 在指定位置插入休止符
    
    def delete_event(text: str, event_index: int) -> str
        # 删除指定音符事件
```

**特性:**
- 正确识别和弦 `[1 3 5]-` 作为单个事件
- 支持所有时值后缀 `_` `-` `.` `·`
- 支持半音标记 `#`
- 自动跳过调号行 `1=C 4/4` 和歌词行
- 保持多行简谱的换行结构

#### 便捷函数
```python
insert_rest(text, event_index, before, rest_token="0_")  # 插入休止符
delete_event(text, event_index)                          # 删除音符
```

### 界面集成

#### `gui/upload_tab.py`
在步骤3"校对表格"区域新增编辑按钮组:

1. **音符编辑组**
   - ← 前插半拍休止 (灰色次要按钮)
   - 后插半拍休止 → (灰色次要按钮)
   - 删除选中音符 (红色危险按钮)

2. **表格操作组** (原有功能)
   - 添加行
   - 删除选中行
   - 清空表格

**使用流程:**
1. 粘贴简谱 → 点击"解析到校对表格"
2. 在表格中点击选中要编辑的音符行
3. 点击对应编辑按钮
4. 简谱文本和表格自动同步更新
5. 继续编辑或直接保存

### 测试覆盖

创建了完整的单元测试 `tests/test_jianpu_editor.py`:

**测试用例 (19项全部通过):**
- ✅ 简单音符范围提取
- ✅ 带时值后缀的音符
- ✅ 和弦识别
- ✅ 多行简谱处理
- ✅ 插入休止符(前/后)
- ✅ 删除音符
- ✅ 删除和弦
- ✅ 保持换行结构
- ✅ 清理空白符
- ✅ 半音标记支持
- ✅ 边界情况(无效索引/空文本)

运行测试:
```bash
pytest tests/test_jianpu_editor.py -v
# ============================= 19 passed in 0.05s ==============================
```

## 三、用户体验改进

### 演奏控制页
- 窗口任意缩放不会导致控件重叠
- 内容过多时自动出现滚动条
- 所有按钮和信息始终可见

### 上传识别页
- 编辑按钮语义明确,带工具提示
- 按钮颜色区分:灰色(普通操作)、红色(删除操作)
- 编辑后自动重新解析,即时反馈
- 选中状态自动跟随,便于连续编辑

## 四、技术亮点

1. **位置稳定的编辑**
   - 基于事件索引而非字符位置
   - 编辑后索引不会错乱
   - 支持连续多次编辑

2. **智能空白处理**
   - 插入时检查左右是否需要空格
   - 删除时自动清理相邻空白
   - 保持简谱格式整洁可读

3. **格式兼容性**
   - 兼容所有标准简谱记号
   - 保持原有换行和分段
   - 支持调号行和歌词行混排

4. **测试驱动开发**
   - 19个测试用例覆盖核心功能
   - 包含边界情况和异常处理
   - 确保代码质量和稳定性

## 五、后续建议

可选的进一步优化方向(本次未实现):

1. **批量编辑**
   - 选中多个音符批量插入休止符
   - 按条件批量修改时值

2. **撤销/重做**
   - 支持 Ctrl+Z / Ctrl+Y
   - 保存编辑历史栈

3. **快捷键**
   - Insert 键快速插入休止符
   - Delete 键快速删除选中音符

4. **可视化辅助**
   - 表格中高亮显示休止符
   - 用不同颜色区分和弦

## 六、验证步骤

1. 启动应用:
   ```bash
   python main.py
   ```

2. 测试演奏控制页:
   - 缩小窗口高度,确认出现滚动条
   - 所有卡片和按钮正常显示
   - 滚动流畅,无重叠

3. 测试音符编辑:
   - 进入"上传识别"页
   - 粘贴简谱:`1 2 3 4 5 6 7 1'`
   - 点击"解析到校对表格"
   - 选中第3行(音符3)
   - 点击"← 前插半拍休止",确认变为:`1 2 0_ 3 4 5 6 7 1'`
   - 点击"删除选中音符",确认第4个音符被删除
   - 保存入库,演奏验证

## 文件清单

### 新增
- `tests/test_jianpu_editor.py` - 简谱编辑器单元测试

### 修改
- `gui/player_tab.py` - 演奏控制页添加滚动区域
- `gui/upload_tab.py` - 上传页添加音符编辑按钮
- `core/jianpu_editor.py` - 添加 delete_event 方法和便捷函数

---

**更新完成!** 所有功能已测试通过,可以正常使用。
