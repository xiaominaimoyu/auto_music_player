"""简谱识别提示词:供用户复制到任意外部 AI 工具,把乐谱图片转成规范化简谱文本。

本程序不再内置在线大模型识别,统一走「复制提示词 → 外部 AI 识别 → 粘贴回程序」
的免 Key 路径;此提示词是外部模型输出的唯一格式约束。
"""

JIANPU_PROMPT = """你是一位简谱识别专家。请把输入的乐谱内容转成以下严格格式的简谱文本:

规则:
1. 默认只提取歌曲的主旋律/人声旋律;不要混入钢琴左手、低音、鼓点、分解和弦或其他伴奏声部。只有主旋律本身明确要求同时发声时才输出和弦
2. 音高:中音用数字 1-7;高音在数字后加英文单引号 ' (如 5');低音在数字后加英文逗号 , (如 5,);休止符用 0
3. 半音:升半音必须保留,统一写在数字后面 # (如 1#、5'#);不要把 # 当作装饰记号丢弃
4. 时值:不加符号=四分音符;数字后跟一个下划线 _ = 八分音符,两个 __ = 十六分音符;跟一个减号 - = 二分音符,两个 -- = 全音符;附点用 · 加在时值符号后(如 5_· 是附点八分音符)
5. 和弦:多个同时演奏的主旋律音用方括号括起、空格分隔,并按从低到高排列;时值符号加在右括号后(如 [1' 3' 5']- 是二分和弦);和弦内的音只写音高不写时值
6. 如果原图是键盘字母谱,必须先转换:Q/W/E/R/T/Y/U 分别是高音 1'/2'/3'/4'/5'/6'/7';A/S/D/F/G/H/J 分别是中音 1/2/3/4/5/6/7;Z/X/C/V/B/N/M 分别是低音 1,/2,/3,/4,/5,/6,/7,;转换后不要保留任何键盘字母
7. 每个音符(或和弦)之间必须用空格分隔,即使原图中的字母或数字连写也要拆开;禁止输出 7_1、17_1、7_7_1222 这类粘连记号
8. 英文逗号 , 只表示低音;下划线 _ 只表示时值,绝不能用下划线代替低音记号
9. 小节线、调号(如 1=C)、拍号、歌词、装饰音符号、力度记号全部丢弃,只输出音符序列
10. 只输出简谱本身,不要输出任何解释文字,不要用代码块包裹

示例输入(图片或文字):[简谱]
示例输出:1 1 5 5 6 6 5- 4 4 3 3 2 2 1-
"""
