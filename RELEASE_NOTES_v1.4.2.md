# Auto Music Player v1.4.2

v1.4.2 专注修复“乐谱内容正确但游戏内听起来像乱弹”以及普通播放路径的演奏记录不可见问题。该版本保持既有公开乐谱格式和数据库兼容性，不移动历史 v1.4.1 标签或覆盖其发布资产。

## 演奏可辨识度修复

- 自动 MIDI 选轨优先明确的主旋律、人声或 Lead 轨，并结合单声部比例、可演奏时值和事件密度，避免误选高音伴奏或碎片轨。
- MIDI 导入及既有 MIDI 乐谱播放时，会将短于 60ms、游戏难以稳定采样的碎片并入相邻旋律，同时保持歌曲总时长不变。
- 导入 MIDI 演奏时不再叠加额外按键间隔和真人化抖动，避免短音密集歌曲逐音累积改拍。
- 外部 AI 提示词明确只提取主旋律或人声旋律，排除钢琴左手、低音、鼓点、分解和弦及其他伴奏声部。
- 三角洲行动口琴不支持和弦时改为保留最高旋律音，并去除重复按键。
- 检测到前台游戏与所选 Profile 明确不匹配时，会在发送按键前停止并提示切换档位。

## 演奏日志修复

- 默认 21 键 `Player` 与三角洲 `EventPlayer` 共用 `data/play_logs` 事件日志目录和会话格式，演奏记录页可以读取两条路径的新日志。
- 日志记录包含会话开始、音符执行、停止或异常以及完整会话结束信息。
- 旧版本已经生成在 `data/logs` 的兼容统计日志仍会保留，但不会自动迁移成新事件日志。

## 版本与升级行为

- 主窗口和 `version.txt` 已升级为 v1.4.2。
- “下次启动不再弹出”设置按 v1.4.2 独立保存；v1.4.1 或更早版本的选择不会静默跳过本版本首次风险提示。
- SQLite 既有乐谱结构和公开简谱格式保持兼容，无需迁移数据库。

## 验证结果

- 版本/免责声明定向回归：`21 passed`。
- 完整 unittest：`436 tests, OK (skipped=2)`。
- 完整 pytest：`504 passed, 2 skipped, 14 subtests passed`。
- 离屏 GUI 冒烟：`GUI smoke OK`。
- Windows 单文件候选包连续运行 6 秒，主/子进程均保持存活；随后按精确进程路径清理，未留下残余进程。
- 包内检查：v1.4.2 版本标识、免责声明键、本次修复模块、默认配置、2 个 Profile 与第三方许可均存在；`icu*.dll` 为 0 个。

## Windows 发布产物

- 文件：`AutoMusicPlayer.exe`
- 大小：`28,685,373` 字节（约 27.36 MiB）
- SHA-256：`FCC0CE52EB5BE7B2B4EEB83DF8B62002C8E671638B65FABFF182D75E93C015A6`
- Authenticode：`NotSigned`
- 构建环境：Windows 11、Python 3.14.7、PyInstaller 6.22.2、PyQt6/Qt 6.11.0、mido 1.3.3

## 注意事项

- 首次启动会显示风险提示和 3 秒阅读锁定；自动演奏可能违反目标游戏规则，请自行评估账号和环境风险。
- 正式演奏前请确认目标窗口、权限级别和游戏档位正确；如果使用《三角洲行动》，请选择“三角洲行动 · 守夜人口琴”档位。
- MIDI 转换为有限音域游戏谱属于有意改编，不保证无损还原全部声部、力度、踏板或演奏法。
- 当前 Windows EXE 未做代码签名，系统可能显示安全提示；升级前建议备份 `data/scores.db`。
- 自动化测试、普通权限启动和包内容检查不等同于真实游戏听感、管理员权限/UIPI、反作弊环境或长期演奏验收；请先在低风险环境短段复测。
