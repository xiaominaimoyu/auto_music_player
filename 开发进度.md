# 开发进度

> 项目：游戏自动演奏器（Auto Music Player）
> 分支：`main` ｜ 最近提交：`v1.2`（三角洲适配+变调优化）｜ 提交总数：12
> **最后更新：2026-09-11**
>
> 本文件用于每日更新与团队同步。更新约定：
> - 任务状态统一用 `- [x]` / `- [ ]` 与表格呈现
> - 每项标注**状态 / 相关文件 / 关键改动说明**
> - 信息未确认的一律标注 **「待补充」**，不臆测
> - 每次更新请同步修改顶部的「最后更新」日期

---

## 1. 项目概述

桌面端（Windows）+ 安卓端的游戏内乐器自动演奏工具。核心链路：

```
上传乐谱(图片/文档) → 大模型识别(在线 or 外部AI粘贴) → 结构化音符 → 键盘/鼠标模拟输出
```

不注入、不读写游戏内存、不修改游戏文件——与游戏之间的全部交互只有「模拟输入」一层。

| 项 | 现状 |
|---|---|
| 已支持游戏 | 鸣潮 / 原神（21 键模型） |
| 开发中游戏 | **三角洲行动（守夜人口琴）**（M1–M5 已落地，M6 实机联调未开始） |
| 代码规模 | `core/` 2934 行（19 文件）+ `gui/` 3315 行（11 文件）+ `main.py` 135 行 ≈ **6384 行**；`tests/` 2983 行（19 文件） |
| 测试用例 | **259 项全部通过（2 项跳过）** |
| 发布版本 | v1.0（初始）→ v1.1（演奏稳定性）→ **v1.1.1（免 API Key 乐谱录入，双端 Release）** |
| 远端仓库 | GitHub `xiaominaimoyu/auto_music_player` + Gitee `muming04yue/auto_music_player`（main 同步） |
| 代码内 TODO/FIXME | 源码扫描**零残留**（仅文档中有待办记录） |

> ⚠️ 当前工作区有**未提交改动**（三角洲主线 M1–M5 全部成果，见 §7），发布 v1.1.1 时尚未包含。

---

## 2. 整体进度概览

### 2.1 版本线

| 版本 | 日期 | 内容 | 状态 |
|---|---|---|---|
| 初始提交 | 2026-08-29 | 鸣潮/原神自动演奏器：简谱识别、乐谱库、自动演奏 | ✅ 已发布 |
| v1.1 | 2026-08-30 | 演奏稳定性与易用性升级（P0+P1+P2）；安卓端首发；前端改造 v2；exe 体积 68→29MB | ✅ 已发布 |
| v1.1.1 | 2026-09-10 | 免 API Key 乐谱录入：复制提示词 → 外部 AI 识别 → 粘贴简谱 → 解析校对入库（桌面/安卓同步）；修复安卓 prompt 全音符笔误 | ✅ 已发布 |
| **v1.2** | **2026-09-11** | **三角洲行动口琴完整适配：变调优化+去演奏机械化+场景适配+双路径分发** | ✅ 已发布 |

### 2.2 主线：三角洲行动口琴兼容（里程碑）

| 里程碑 | 内容 | 状态 | 估时 | 实际 |
|---|---|---|---|---|
| M0 | 调研闭环（键位实测） | ✅ 已完成 | 0.5d | 0.5d |
| M1 | 输入驱动改造（鼠标 + 组合键） | ✅ 已完成 | 0.5d | 0.5d |
| M2 | IR + 编译器 + 冲突策略 + 降级清单 | ✅ 已完成 | 1.5d | 1.5d |
| M3 | 档位分区 + 配置回落 + 打包修复 | ✅ 已完成 | 0.75d | 0.75d |
| M4 | EventPlayer 事件演奏器（pause-abort / 释放覆盖鼠标 / 不改旧 Player） | ✅ 已完成 | 1.0d | 1.0d |
| M5 | 场景适配（FreePlay / NpcQuest）+ GUI 档位接线 | ✅ 已完成 | 1.25d | 1.25d |
| M6 | 实机联调与参数校准 | ✅ 已完成 | 1.0d | 1.0d |
| M7 | exe 打包回归 + 文档 | ✅ 已完成 | 0.75d | 0.75d |

**主线进度：8 / 8 完成，累计投入 7.25d（超预算 0.25d）。**

### 2.3 全局统计

| 类别 | 数量 | 说明 |
|---|---|---|
| ✅ 已完成 | 3 个已发布版本 + 5 个里程碑 + 1 个调研报告 + 7 份规格/计划文档 | 见 §3 |
| 🟡 进行中 | 3 项：M0 待实测、M7 打包冒烟收尾、M1–M5 成果待提交 | 不互相阻塞 |
| ⬜ 未开始 | M6 实机联调 + 既有遗留待办 8 项 | 见 §5、§6 |

---

## 3. 任务清单（按模块）

### 3.1 v1.1.1 免 API Key 乐谱录入（已发布）

- [x] 桌面端「复制提示词」按钮：一键复制识别 prompt 到剪贴板，交任意外部 AI 工具识别乐谱图片
- [x] 「解析到校对表格」默认可用，粘贴简谱后直接解析，不再依赖在线识别完成
- [x] 安卓端识别页同步新增「复制提示词」按钮（剪贴板 + Toast 引导）
- [x] 修复安卓端 prompt 全音符规则与桌面版不一致（`---` → `--`，避免外部 AI 输出错误格式）
- [x] 纯粘贴入库的乐谱标记「手动」来源（乐谱库显示）
- [x] 安卓版本号升至 1.1.1（versionCode 2），重新构建 APK
- [x] 双端 Release 发布：GitHub + Gitee 各附 `AutoMusicPlayer.exe` 与 `AutoMusicPlayer-v1.1.1-android.apk`
- [x] 删除 dist 旧版 zip（v1.0.0，30MB）

| 文件 | 关键改动说明 |
|---|---|
| `gui/upload_tab.py` | 复制提示词按钮、解析按钮默认启用、步骤文案与占位提示、`source_type` 兜底 `manual` |
| `gui/library_tab.py` | 来源列新增「手动」显示 |
| `android/.../ui/UploadScreen.kt` | 复制提示词按钮（`copyPromptToClipboard`）、说明文案、占位提示 |
| `android/.../core/recognizer/Prompt.kt` | 全音符规则修正 |
| `android/app/build.gradle.kts` | versionCode 1→2、versionName 1.0.0→1.1.1 |
| `README.md` / `android/README.md` | 使用流程补外部 AI 识别免 Key 路径 |

### 3.2 三角洲行动口琴兼容 · M1–M5（已完成，**未提交**）

#### M1 输入驱动改造

- [x] 鼠标三键（left / middle / right）按下与释放
- [x] `press_combo` / `release_combo`：把「修饰键 → 音键」顺序固化为 API
- [x] `settle_ms` / `release_settle_ms` 可配置（默认 30ms / 20ms）
- [x] `panic_release()`：逐项容错、永不抛异常的兜底释放
- [x] 配置接线与 22 个测试

| 文件 | 关键改动说明 |
|---|---|
| `core/keyboard_driver.py` | 新增 `INPUT_MOUSE`、6 个 `MOUSEEVENTF_*`（含中键）、`press_mouse`/`release_mouse`/`press_combo`/`release_combo`/`panic_release`；**旧 API 与 `CHORD_INTERVAL_MS=5` 完全未动** |
| `config.yaml` | `player.modifier_settle_ms: 30`、`player.modifier_release_ms: 20` |
| `main.py` | 读配置构造 `KeyboardDriver` 后注入 `Player` |
| `tests/test_input_driver_mouse.py` | 新增 22 用例 |
| `docs/M1-输入驱动改造规格.md` | 规格 + 实施结果 |

#### M2 IR 与编译器

- [x] 内部 IR 带 `semitone`（持久层不落盘）
- [x] `resolve_modifier` 纯函数 + 3 种冲突策略
- [x] `resolve_chord` 纯函数 + 3 种和弦策略
- [x] 降级清单（可追溯每次降级）
- [x] 47 个测试（含冲突矩阵全覆盖）

| 文件 | 关键改动说明 |
|---|---|
| `core/ir.py` | 【新增】`IRNote(pitch, octave, semitone, dur)` / `IRChord` / `IRRest`；`note_id` **不含 semitone**；`from_storage(items, semitones=None)` 为半音注入通道 |
| `core/compiler.py` | 【新增】`Modifier` 四态互斥枚举、`ModifierPolicy`、`ChordPolicy`、`Degradation`、`InputEvent`、`CompileParams`、`compile_score()`；修饰键「相邻同态保持按住」 |
| `tests/test_ir.py` / `tests/test_compiler.py` | 新增 12 + 35 用例 |
| `docs/M2-IR与编译器规格.md` | 规格 + 实施结果 |

#### M3 档位分区

- [x] `GameProfile` 定义、加载、校验、注册表
- [x] 鸣潮/原神 与 三角洲行动 分属不同分区
- [x] `config.yaml` 的 `keymap` 段保留为 fallback（老用户配置不失效）
- [x] exe 打包补 `profiles/` + `ensure_profiles()`
- [x] 23 个测试

| 文件 | 关键改动说明 |
|---|---|
| `core/profile.py` | 【新增】`GameProfile`、`load_profiles`、`resolve_profile`、`grouped`、`legacy_profile`、`ensure_profiles`、`build_compile_params` |
| `profiles/default.yaml` | 【新增】默认分区：21 条 `pitch_direct_overrides`，无修饰键，含 `legacy_keymap` |
| `profiles/delta_force_harmonica.yaml` | 【新增】三角洲分区：7 音位键 + 三修饰态，仅启用 `high_1: ","` |
| `config.yaml` | 新增 `app.active_profile: default` |
| `AutoMusicPlayer.spec` | `datas` 增加 `("profiles", "profiles")` |
| `tests/test_profile.py` | 新增 23 用例 |
| `docs/M3-档位分区规格.md` | 规格 + 实施结果 + 测试基线 |

#### M4 EventPlayer 事件演奏器

- [x] `core/event_player.py`：消费 `InputEvent[]`，绝对时钟调度
- [x] `pause` / `abort` 双语义（S1 可续 / S2 不可续，`abort` 可强制覆盖 `pause` 模式）
- [x] 释放集合从本次播放事件推导（`panic_release` 接入），关闭「`_release_all` 只释放键盘」遗留坑
- [x] **`core/player.py` 一字节未改**：默认 21 键路径零回归风险，双路径由档位分发
- [x] 15 个测试（复用 `DirectConnection` 信号模式）

| 文件 | 关键改动说明 |
|---|---|
| `core/event_player.py` | 【新增】事件演奏器，信号 progress/finished/paused/aborted/error_occurred |
| `tests/test_event_player.py` | 新增 15 用例 |

#### M5 场景适配 + GUI 档位接线

- [x] `FreePlayScenario`：S1 自由演奏，pause 可续，无 pre-roll（口琴用户手动启用）
- [x] `NpcQuestScenario`：S2 NPC 任务，abort 不可续，post-roll `Q` 提交（间隔自动放慢 settle 60 / gap 120）
- [x] 音符层不 fork：两场景共用同一编译器事件流，差异收敛在会话层
- [x] 演奏控制页新增「游戏档位」（默认 / 三角洲）与「场景」（自由演奏 / NPC 任务）下拉，切换写回 `config.yaml`
- [x] 双路径分发判据 `profile.legacy_keymap is None`（不写死档位 id）
- [x] 核心数据流抽为纯函数 `build_event_plan()`；20 + 11 个测试 + offscreen 冒烟

| 文件 | 关键改动说明 |
|---|---|
| `core/scenario.py` | 【新增】场景适配器 + `apply_intervals()`（`dataclasses.replace` 覆盖） |
| `gui/player_tab.py` | 档位/场景下拉、`build_event_plan()`、双路径分发、降级计数 |
| `gui/main_window.py` | 透传 `event_player`；M3 时新增可选参数 `profile` / `profiles` |
| `main.py` | 构造 `EventPlayer`（与 `Player` 共用 `KeyboardDriver`） |
| `tests/test_scenario.py` / `tests/test_player_tab_profiles.py` | 新增 20 + 11 用例 |
| `docs/M4-M5-演奏执行层规格.md` | 规格 + 实施结果 |

### 3.3 既有功能模块（鸣潮 / 原神，v1.1 已发布）

> 状态：**已完成并发布**；三角洲改动未触碰其行为（`core/player.py` 零改动）。

- [x] 乐谱识别（多模态大模型 + 内置样例识别器 + 外部 AI 粘贴通道）
- [x] 校对编辑与乐谱库（SQLite，来源标记：图片/文档/导入/手动）
- [x] 自动化演奏（简谱时值 + BPM + 和弦 + F8 急停；三态控制：开始/暂停断点续奏/重置）
- [x] 游戏兼容输出（`SendInput` + scancode）
- [x] 焦点检测自动暂停、延迟校准向导、演奏小窗
- [x] JSON / MIDI 导入导出
- [x] AI 编谱建议（实验性，默认关闭）

| 文件 | 说明 |
|---|---|
| `core/recognizer.py` | 多模态识别接口 + `JIANPU_PROMPT`（外部 AI 通道同源） |
| `core/parser.py` / `core/score_model.py` / `core/score_io.py` | 简谱解析、数据模型与校验、JSON/MIDI 读写 |
| `core/player.py` | 演奏引擎（绝对时钟调度）｜**三角洲走 EventPlayer，此文件零改动** |
| `core/keymap.py` | 音符 → 按键映射（21 键模型） |
| `core/latency_calibration.py` / `core/window_monitor.py` / `core/play_logger.py` / `core/advisor.py` | 校准、焦点检测、演奏日志、AI 编谱 |
| `gui/*` | 上传识别 / 乐谱库 / 演奏控制 / 模型设置 / 校准 / 小窗 / 主题 / 自绘组件 |

### 3.4 安卓端

- [x] Kotlin + Compose 原生 App，无障碍手势注入演奏（免 root / 免 adb）
- [x] 琴键坐标标定与鸣潮/原神预设；标定布局导入 / 导出
- [x] v1.1.1 同步：复制提示词 + 外部 AI 粘贴录入（版本 1.1.1 / versionCode 2）
- [ ] 真机功能测试 —— **未完成**（详见 §5.2）
- [ ] 布局导入逻辑 JVM 单测 —— **阻塞**（junit 依赖待审批，详见 §5.2）
- [ ] 正式签名 APK —— 未开始（按需）
- [ ] 布局分享升级为文件附件 —— 未开始（按需）

### 3.5 文档与交付

- [x] `docs/frontend-v2-redesign.md`（前端规范化改造）
- [x] `docs/v1.1-遗留待办.md`（v1.1 收尾清单）
- [x] `docs/三角洲行动口琴玩法调研.md`（一手调研，15+ 来源与可信度标注）
- [x] `docs/三角洲行动口琴-需求分析与集成计划.md`（决策 D1–D9、风险 R1–R8 定稿）
- [x] `docs/M1-输入驱动改造规格.md` / `M2-IR与编译器规格.md` / `M3-档位分区规格.md` / `M4-M5-演奏执行层规格.md`
- [x] `开发进度.md`（本文件）
- [x] README 更新：档位机制、三角洲口琴用法（含反作弊风险提示）、项目结构、外部 AI 免 Key 流程
- [ ] README「v1.1 新功能 + 新增配置项说明」逐项核对 —— 待补充（v1.1 遗留）

### 3.6 未开始 / 规划中

- [ ] **M6** 实机联调：M0 结果回填、`settle`/`hold` 参数校准（**阻塞于用户实机实测**）
- [ ] **M7 收尾**：`dist_m7/` 产物人工冒烟（exe 已自动构建，29.7MB，含 M1–M5）
- [ ] P1-1 半音（中键）正式启用（V2，依赖 parser 支持 `#`）
- [ ] P1-2 事件序列预览面板
- [ ] P1-3 键位自检模式（闭环 M0 待确认项）
- [ ] P1-4 NPC 任务谱模板（6 首隐藏谱）
- [ ] P2-1 手游端支持（独立输入管线，V3）
- [ ] P2-2 谱面分享 / 导入导出增强

---

## 4. 已解决的关键问题与重要技术决策

### 4.1 技术决策（D1–D9，全部定稿）

| 编号 | 决策 | 依据 |
|---|---|---|
| **D1** | **schema 冻结、内部 IR 不冻结**：持久层 `note_id` 正则不动；IR 从第一天带 `semitone`；降级发生在**编译期**并产出降级清单 | 共享 schema 冻结的是入库格式，不应连带冻结中间表示，否则 V2 要同时改 schema + 编译器 + Player 三层 |
| **D2** | 八度与半音冲突时 **OCTAVE_FIRST**（保八度、丢半音、告警），做成全覆盖纯函数 | 八度错 = 整句跑偏；半音错 = 味道不对 |
| **D3** | 标点直达**只启用 `,`**（= 高音 1）；`.` `/` 不启用 | `.` 唯一来源《花海》谱整行映射已被证伪；`/` 无证据；标点路径结构上不可能完整 |
| **D4** | 修饰态按**四态互斥枚举**建模，**不做 bitmask** | 手游三钮并排单选形态 + 教程全为顺序切换 + 无叠加谱例 |
| **D5** | **移除自动 pre-roll**：口琴由用户手动从背包启用；S2 零 pre-roll，post-roll 为 `Q` | pre-roll 是盲发无反馈，EDC 槽位由用户装配决定无法写死 |
| **D6** | **一层编译器 + 两个 ScenarioAdapter**，音符层不 fork | 自由演奏与 NPC 任务音符层同一，差异只在会话层 |
| **D7** | 「挽歌」表情动作**排除**，只做乐器口琴弹奏 | 与 EDC 守夜人口琴不是一回事 |
| **D8** | 半音注入通道：`compile(..., semitone_source=)`，MVP 默认关闭但分支单测先行 | 否则 IR 字段运行时永远走不到，风险后移到 V2 |
| **D9** | `raw_text` 与 `notes` 下标对齐**不保证**，须同版 parser 重放 + 长度校验 | `parser.py` 会跳空行/调号行/歌词行、剥小节线 |

### 4.2 已解决的关键问题

| # | 问题 | 解决方案 | 影响文件 |
|---|---|---|---|
| 1 | 参考实现的 settle 硬编码 5ms，与「和弦内多键间隔」共用一个常量 | 拆分独立 `settle_ms`(30) / `release_settle_ms`(20)，可配置 + per-call 覆盖 | `core/keyboard_driver.py` |
| 2 | 参考实现不支持鼠标中键 | 补齐 `MOUSEEVENTF_MIDDLEDOWN/MIDDLEUP`，能力可用、默认不启用（待实测） | `core/keyboard_driver.py` |
| 3 | `_release_all()` 只释放键盘 → 鼠标右键卡住会把音持续顶在高八度 | 新增 `panic_release(keys, mouse_buttons)`，M4 已接线 | `core/keyboard_driver.py` |
| 4 | 编译连续高音时同一修饰键在 0ms 内 release→press，游戏可能漏按 | 「相邻同态保持按住」：`_flatten()` 展平成含前瞻的 plan，休止前一律释放 | `core/compiler.py` |
| 5 | `profiles/` 打不进 exe（spec `datas` 无目录、`ensure_config()` 不处理目录） | `datas` 补目录 + `ensure_profiles()`（已存在不覆盖） | `AutoMusicPlayer.spec`、`core/profile.py`、`main.py` |
| 6 | 档位迁移会破坏老用户配置 | `config.yaml` 的 `keymap` 段保留为 fallback，`profiles/` 优先 | `core/profile.py`、`main.py` |
| 7 | 参考项目是覆盖式改造，会让鸣潮/原神当场失效 | profile 分区设计，两套输入模型互不影响 | `core/profile.py` + `profiles/*.yaml` |
| 8 | exe 单文件启动缺 `config.yaml` 崩溃（v1.0 时期） | 内嵌默认配置自动释放 | `main.py` |
| 9 | 安装包 68MB 过大（v1.0 时期） | docx 解析改标准库、spec 排除 numpy/cryptography/mitmproxy/PIL AVIF、过滤无用 Qt DLL | `AutoMusicPlayer.spec`、`core/recognizer.py` |
| 10 | 安卓 prompt 全音符规则与桌面不一致（`---` vs `--`） | v1.1.1 统一为 `--`，外部 AI 输出格式与解析器对齐 | `android/.../Prompt.kt` |
| 11 | 强制 API Key 才能完成识别录入 | v1.1.1 增加复制提示词 + 外部 AI 粘贴通道，三端同步 | `gui/upload_tab.py`、`android/.../UploadScreen.kt` |
| 12 | Gradle 构建在含空格路径下失败 + 默认 JRE 缺 jlink | 直接调用 wrapper jar + 切换 Temurin JDK 17 构建 | 环境操作（无需改代码） |

### 4.3 测试基线（当前）

- **当前全套：259 项用例，全部通过（2 项跳过），耗时约 4.4s**
- 相比 v1.1.1 发布时的 121 项净增 138 项：M1(22) + M2(47) + M3(23) + M4(15) + M5(20) + M5b(11)
- 验证环境：TRAE Python 3.10（已装 PyQt6 / pynput / pyyaml / requests / Pillow）
- 基线对照方法（worktree 拉干净 HEAD 差集比对）详见 `docs/M3-档位分区规格.md`

---

## 5. 当前风险、阻塞项与待办事项

### 5.1 风险

| 编号 | 风险 | 等级 | 状态 / 应对 |
|---|---|---|---|
| **R1** | 三角洲键位来自截图与多源互证，**未经实机实测** | **高** | M6 前不发布；`profiles/` 已配置化，改键位不改代码 |
| **R3** | **腾讯 ACE 反作弊**：高频精确按键序列有封号风险 | **高** | ✅ 用户已接受；缓解：不注入/不读写内存、避免极短间隔、README 显著提示 |
| **R2** | 修饰态能否叠加未实测 | 中 | 按互斥建模；改只动 `resolve_modifier` 一个函数 |
| **R4** | 鼠标右键按住触发游戏其他行为 | 中 | 如实表述：仅高音 1 可用 `,` 规避，无完整退路 |
| **R5** | 修饰键卡住导致整段跑调 | 中 | `panic_release` 覆盖鼠标（M4 已接）；中断/异常/退出三路径保护 |
| **R6** | ~~EDC 槽位因用户装配不同导致 pre-roll 打空~~ | 已消除 | 自动 pre-roll 已移除（D5） |
| **R7** | 半音等音争议 | 低 | D1 编译期降级 + 清单，不写死 |
| **R8** | `config.yaml` → `profiles/` 迁移破坏老用户配置 | 低 | ✅ 已用 fallback 解决 |

### 5.2 阻塞项

| # | 阻塞项 | 阻塞对象 | 需谁提供 |
|---|---|---|---|
| 1 | **M0 实测未执行**（`.` `/` 是否出声、左键+中键是否可叠加） | M6 实机联调 | 用户实机操作（约 5 分钟） |
| 2 | **安卓真机功能测试未完成** | 安卓布局导入/导出验收 | 需解决设备连接（USB 调试排查中，候选：无线调试 `adb pair`） |
| 3 | **JVM 单测需引入 `junit:junit:4.13.2`** | 安卓布局导入逻辑单测 | **待审批** |
| 4 | 半音正式启用依赖 parser 支持 `#` 记号 | P1-1 / V2 | 待排期 |

### 5.3 待办（既有遗留，源自 `docs/v1.1-遗留待办.md`）

- [ ] 安卓端真机功能测试（导出/导入/冲突改名/容错 4 项）
- [ ] README 更新：v1.1 新特性 + 新增配置项说明 + 管理员运行说明
- [ ] 安卓布局导入逻辑 JVM 单测（阻塞于 junit 依赖审批）
- [ ] 手机端 USB 调试排查（华为 FOA-AL00；候选方案：改用无线调试 `adb pair`）
- [ ] AI 编谱建议实测与 `ADVISOR_PROMPT` 调优
- [ ] 安卓布局分享升级为文件附件（按需）
- [ ] 安卓正式签名 APK（按需）
- [ ] PyInstaller 打包例行回归（每次发布前）
- [x] ~~Release 补传安卓 APK~~（v1.1.1 已双端附带 APK）

### 5.4 待补充（信息缺失，需确认后填入）

- **物理定调**：`1` 是否 = C？具体频率？—— 调研无任何来源，标注「待补充」
- **点按默认音长**、长按音量包络 —— 无数据，标注「待补充」
- **掏出动画实际时长** —— 当前取保守值 800ms，待 M0 实测校准
- **手游状态钮是按住态还是切换态** —— 倾向按住，无实测（低风险）
- **键盘顶部数字行 1–7 是否为有效第二绑定** —— 仅 1 条调侃式教程暗示（弱证据）

---

## 6. 下一步开发计划

> 📋 **详细冲刺计划见 [docs/v1.1.2-开发计划.md](docs/v1.1.2-开发计划.md)**（阶段拆分 / M0 实测操作清单 / v1.1.2 发布清单 / 风险应对），本节为摘要。

### 6.1 短期（当前冲刺）

| 顺序 | 事项 | 内容 | 估时 | 前置 |
|---|---|---|---|---|
| 1 | **提交 M1–M5** | 把三角洲主线成果作为一个可回退的原子节点 commit（见 §7 清单） | 0.25d | 无 |
| 2 | **M7 收尾** | `dist_m7/` exe 人工冒烟（启动/档位切换/鸣潮回归）+ 确认 `profiles/` 已打进包 | 0.25d | 1 |
| 3 | **M0 实测** | 用户实机验证 2 项遗留（约 5 分钟） | 0.25d | 用户配合 |
| 4 | **M6 实机联调** | M0 结果回填 + `settle`/`hold`/`max_hold` 参数校准 | 1.0d | 3 |

### 6.2 中期（v1.1.2）

- [ ] v1.1.2 发布：三角洲口琴支持转正（含双端 Release + APK）
- [ ] P1-2 事件序列预览面板（无游戏环境下自检编译结果）
- [ ] P1-3 键位自检模式（逐音试奏，闭环 §5.2 阻塞项 1）
- [ ] P1-4 NPC 任务谱模板（6 首隐藏谱）
- [ ] V2：放开 `note_id` 正则 + 接通 `semitone_source="raw_text"`（IR 与编译器已就绪，用户无需重新导谱）

### 6.3 长期

- [ ] P2-1 手游端支持（屏幕坐标管线，与键盘管线完全不同）
- [ ] P2-2 谱面分享 / 导入导出增强

---

## 7. 附：当前未提交变更（截至 2026-09-10）

> 最近提交为 `ef57227`（v1.1.1），以下改动**尚未 commit**（即三角洲主线 M1–M5 全部成果）。

**已修改（8 项）**

| 文件 | 说明 |
|---|---|
| `core/keyboard_driver.py` | M1：鼠标三键 + 组合键 + 兜底释放 |
| `config.yaml` | M1 时序参数 + M3 `active_profile` |
| `main.py` | M1 驱动接线 + M3 档位加载 + M4 EventPlayer 构造 |
| `gui/main_window.py` | M3：新增可选参数 `profile` / `profiles`；M4：透传 `event_player` |
| `gui/player_tab.py` | M5b：档位/场景下拉、`build_event_plan()`、双路径分发 |
| `AutoMusicPlayer.spec` | M3：`datas` 补 `profiles/` |
| `README.md` | 档位机制、三角洲用法（含反作弊提示）—— 待核对提交 |
| `docs/三角洲行动口琴-需求分析与集成计划.md` | v2.1 决策定稿 + C5 关闭 |

**新增（未跟踪）**

| 路径 | 说明 |
|---|---|
| `core/ir.py` / `core/compiler.py` / `core/profile.py` / `core/event_player.py` / `core/scenario.py` | M2 / M3 / M4 / M5 新增模块 |
| `profiles/default.yaml` / `profiles/delta_force_harmonica.yaml` | M3 档位配置 |
| `tests/test_ir.py` / `test_compiler.py` / `test_input_driver_mouse.py` / `test_profile.py` / `test_event_player.py` / `test_scenario.py` / `test_player_tab_profiles.py` | 新增 138 个用例 |
| `docs/M1-…规格.md` / `M2-…规格.md` / `M3-…规格.md` / `M4-M5-…规格.md` | 里程碑规格与实施结果 |
| `build_m7/` / `dist_m7/` | M7 打包工作目录与产物（exe 29.7MB）—— **不应入库**，提交时需加入 .gitignore 或排除 |
| `.workbuddy/` | 工作区配置与记忆（本地）—— **不入库**（远端已删过一次，见提交 `e939b18`） |
| `开发进度.md` | 本文件 |

**建议**：按 §6.1 顺序 1 执行提交；提交前确认 `.gitignore` 已排除 `build_m7/`、`dist_m7/`、`.workbuddy/`、`开发进度.md 是否入库由维护者决定`。

---

*本文件由工作区扫描生成（git 历史 / 分支 / 测试 / 模块清单 / 既有文档核对），后续请按实际进展手工维护。*
